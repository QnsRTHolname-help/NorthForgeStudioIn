node_modules
dist
.DS_Store
*.local
.env
.env.production
coverage

# Secrets — never commit credentials (Supabase, SMTP, JWT, provider keys)
.env
.env.*
!.env.example
*.pem
*.key

# SSH keys — never commit credentials of any kind
.ssh/

# Package manager caches and build artefacts
.npm/
.cache/
.pnpm-store/
tsconfig.tsbuildinfo
*.tsbuildinfo

# Supabase CLI local state (project link cache)
supabase/.temp/
